curl
apt-transport-https ca-certificates gnupg2 software-properties-common
git
vim
zsh
acl
aria2
armbian-config
docker-ce docker-ce-cli docker-compose
glusterfs-server rpcbind
hdparm
htop
iotop
lvm2

systemctl --type=service | grep -i docker | grep -i running
systemctl --type=service | grep -i rpcbind | grep -i running
systemctl --type=service | grep -i glusterd | grep -i running

cat /lib/systemd/system-shutdown/odroid.shutdown

ls -la /tmp
