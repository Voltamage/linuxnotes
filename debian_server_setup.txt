https://oph.mdrjr.net/meveric/images/Stretch/Debian-Stretch-1.0~RC2-20180403-XU3-XU4.img.xz
use SD Card Formatter to format
use etcher.io to burn
defaults: user:root
          password: odroid

apt-get update && apt-get upgrade && apt-get dist-upgrade
apt-get install console-setup keyboard-configuration
dpkg-reconfigure locales
dpkg-reconfigure keyboard-configuration
dpkg-reconfigure tzdata
passwd
nano -w /etc/hostname
reboot
apt-get install ranger neofetch sudo

adduser voltamage
nano -w /etc/sudoers
add line voltamage  ALL=(ALL:ALL) ALL under the line that says root
exit and log back in as voltamage

nano -w ~/.bashrc
add
# Custom commands
neofetch
to the bottom

sudo apt-get --purge autoremove setup-odroid mc vim
